name: CI
on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "**"
  workflow_call:
  workflow_dispatch:

defaults:
  run:
    shell: bash # https://github.com/beeware/briefcase/pull/912

jobs:
  install-briefcase:
    name: Install Briefcase
    runs-on: macos-latest
    steps:

    - name: Set up Python
      uses: actions/setup-python@v4.3.0
      with:
        python-version: "3.10"

    - name: Install Briefcase
      uses: ./.github/actions/install-briefcase

  #  verify-apps:
  #  name: Build apps
  #  uses: rmartin16/.github-beeware/.github/workflows/app-build-verify.yml@briefcase-from-pr-body
  #  with:
  #    python-version: ${{ matrix.python-version }}
  #    runner-os: windows-latest
  #    framework: ${{ matrix.framework }}
  #    target-platform: windows
  #    target-format: app
  #  strategy:
  #    fail-fast: false
  #    matrix:
  #      python-version: ["3.8", "3.9", "3.10", "3.11"]
  #      framework: ["toga", "pyside2", "pyside6", "ppb"]
  #      exclude:
  #      # PySide2 doesn't publish a binary wheel that is compatible
  #      # with Python 3.11, and is unlikely to ever do so.
  #      - python-version: "3.11"
  #        framework: "pyside2"
